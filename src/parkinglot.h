#ifndef MATH_UTILS_H
#define MATH_UTILS_H

namespace ParkingLot {
    class MyStack {
        void mergeSort();
    };
    
    class MyQueue{

    };

    class Car {
    private:
        string carId;
        string type;
    public:
        
    };

    class Parkinglot {
    private:
    public:
        void insert(Car car);
        void insertAt(Car car, int i);
        void sort(int i);
        void move(int i, int j);
        void pop(Car car);
    };

    //free
    
}

#endif